// Copy this file to `config.js` (same directory) and paste your sandbox
// Mini App clientId. The real `config.js` is gitignored so the ID never
// gets committed.
export const CLIENT_ID = "your-sandbox-mini-app-client-id";
export const ENV = "sandbox";
